<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demo.plugin.sample">

    <application>
        <activity android:name=".PluginActivity" />
        <activity android:name=".AnotherPluginActivity" />

        <service android:name=".PluginService" />
        <receiver android:name=".StaticPluginReceiver">
            <intent-filter>
                <action android:name="com.demo.broadcast" />
            </intent-filter>
        </receiver>
    </application>
</manifest>